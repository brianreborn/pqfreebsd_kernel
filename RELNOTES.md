# RELNOTES

Official notes use FreeBSD `src/RELNOTES` form (plain text, commit IDs,
80-column wrap, tab-indented bodies):

**[RELNOTES](RELNOTES)**

GitHub shows that file as text, which is the intended layout. Do not treat
this Markdown file as a substitute for the canonical `RELNOTES`.
